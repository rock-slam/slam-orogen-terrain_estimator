# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(terrain_estimatorTaskLib)
ADD_LIBRARY(${TERRAIN_ESTIMATOR_TASKLIB_NAME} SHARED 
    ${TERRAIN_ESTIMATOR_TASKLIB_SOURCES})


TARGET_LINK_LIBRARIES(${TERRAIN_ESTIMATOR_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${TERRAIN_ESTIMATOR_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${TERRAIN_ESTIMATOR_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${TERRAIN_ESTIMATOR_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${TERRAIN_ESTIMATOR_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${TERRAIN_ESTIMATOR_TASKLIB_HEADERS}
    DESTINATION include/orocos/terrain_estimator)

